rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /projects/{projectId} {
      allow read: if true;
      // Emergency lock: block all client writes until auth-based admin flow is set up.
      allow create, update, delete: if false;
    }
  }
}